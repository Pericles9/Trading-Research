path,resolution,format,naming_conventions,file_count,total_size_bytes,ticker_coverage_summary,date_coverage_summary,flags_or_concerns
data/daily/,"1-day (daily bars)",parquet,"Two coexisting conventions: (1) {TICKER}_daily.parquet - primary corpus, columns [volume,vw,open,close,high,low,timestamp,n,date], n=1836 files; (2) {TICKER}.parquet bare (no suffix) - secondary small watchlist batch, columns [date,open,high,low,close,volume] (no vw/n/timestamp), n=11 files. Plus non-bar reference table universe.parquet (ticker metadata, not OHLCV, n=1 file).",1848,18769605,"~1836 distinct tickers under primary convention (verified via filename parsing); 11 tickers under secondary convention (AAPL AMC AMD GME MARA NVDA PLTR QQQ RIOT SPY TSLA, full overlap with primary set); 144026 total rows in primary convention (exact full-corpus count via read_parquet glob), 1216 rows in secondary convention (exact).","Primary convention: uniform 2024-12-02 to 2025-04-01 (~82 trading days) across all sampled files - verified on 15/1836 random files, all consistent except a couple of partial-history files (e.g. ATMC 30 rows, CLGN 80 rows) ending early within the same window. Secondary convention: 10 of 11 files cover 2025-10-17 to 2025-11-14 (21 rows) but AMD.parquet alone covers 2020-01-02 to 2023-12-29 (1006 rows) - a stale outlier inside an otherwise-recent batch.","Two schemas/conventions coexist in one flat directory for the same resolution (mirrors the trades-corpus pattern from prior audits). AMD.parquet is a schema-consistent but date-range outlier within the 11-file secondary batch (4-year history vs siblings' 1-month window) - looks like a leftover file from a different one-off pull that was never cleaned up. universe.parquet is not OHLCV and does not belong in a naming-convention count for this folder if scripts glob *.parquet indiscriminately. All 1848 files share a single mtime cluster (2025-11-21), consistent with a bulk copy/migration event, not representative of original collection dates."
data/minute/,"1-minute bars (~60s spacing, confirmed via timestamp deltas)",parquet,"Folder-per-symbol, {TICKER}/{YYYY-MM-DD}.parquet, single convention (0/300 sampled tickers had ticker-prefixed filenames). Two schema variants coexist for the SAME bare-filename convention: older files use columns [timestamp,datetime,open,high,low,close,volume,trade_count,vwap]; newer files (2025 dates, e.g. DNOW 2025-02-13, VSTD 2025-09-12) drop 'trade_count' in favor of 'transactions' - column rename only, same meaning, no naming-convention split.",24590,807815225,"3376 ticker directories (excludes the non-ticker 'trades' pseudo-directory - see companion flag row data/minute/trades/). ~7.3 date-files per ticker on average (sparse/event-driven, not continuous daily coverage). 2803 of these tickers also appear in second10/; 846 also appear in daily/_daily convention.","2020-01-03 to 2025-10-31 across 1465 distinct calendar dates (from filenames, full corpus scan, not a sample). Sparse/event-driven: most tickers have a handful of dates clustered around known momentum events, not a continuous trading calendar.","Schema drift within one naming convention: 'trade_count' vs 'transactions' column rename correlates with collection date (transactions used only on ~2025 files) - a pipeline version change, not corruption, but inconsistent schema within one directory. File-level datetime spans occasionally cross a UTC calendar-day boundary relative to the filename date (e.g. minute/AA/2020-03-24.parquet actually contains 2020-03-23 11:00 through 2020-03-24 23:59) - the filename date does not always equal the data's calendar date, same class of naming/content mismatch flagged in prior trades audits. Directory also contains a large embedded non-candle dataset at minute/trades/ (see below) that inflates minute/'s total footprint from ~808MB of real bars to ~91.7GB if not filtered out - anyone globbing data/minute/**/*.parquet for OHLCV bars will silently ingest 18630 raw trade-tick files unless they exclude the trades/ subfolder by name."
data/minute/trades/,"NOT CANDLE DATA - raw trade ticks mis-filed inside the minute/ bar tree",parquet,"Same folder-per-symbol layout as real minute bars: trades/{TICKER}/{YYYY-MM-DD}.parquet, using a literal directory named 'trades' that sits alongside real ticker directories in data/minute/, making it indistinguishable from a ticker by directory listing alone.",18630,90115979078,"2914 ticker subdirectories under minute/trades/ (e.g. AMC, GME, CLOV, BB, SNDL - all well-known 2021 meme-stock names, consistent with high-volume tick dumps).","Not verified in detail (out of scope - this is not bar data); spot-checked files concentrate around 2021 meme-stock squeeze dates (AMC 2021-05-27..2021-06-02, GME 2021-01-25..2021-02-26) and 2024-05-14 (GME/AMC revival rally).","STRONGEST FINDING IN THIS INVENTORY: this is raw Polygon-style trade-tick data (columns: t,p,s,x,c,q,i,participant_ts,timestamp_ms,price,size,exchange,conditions,sequence_number,trade_id,timestamp - confirmed via schema read), not OHLCV bars, sitting inside the minute/ candle directory under a folder named 'trades' that looks like a ticker symbol. Individual files reach up to 396MB (AMC 2021-06-02.parquet, 9M+ rows) and this subfolder alone accounts for 90.1GB of the 90.9GB total footprint under data/minute/ - i.e. real minute-bar candle data is only ~0.9% of what a naive du -sh data/minute/ would report. Anyone treating data/minute/ size or file count as a proxy for bar-data volume will be off by ~110x. Flagged prominently per task instructions; no further sweep performed."
data/second10/,"10-second bars (confirmed via timestamp deltas, min delta=10s in sampled files)",parquet,"TWO coexisting naming conventions in the same ticker directories: (1) bare {TICKER}/{YYYY-MM-DD}.parquet (older, event-driven dates back to 2025-01-03); (2) ticker-prefixed {TICKER}/{TICKER}_{YYYY-MM-DD}.parquet (newer, contiguous daily runs concentrated 2025-10-23 to 2025-11-21). Schema also differs by convention: bare files have columns [timestamp,open,high,low,close,volume,trade_count,vwap,datetime] (includes a proper datetime column); prefixed files have columns [open,high,low,close,volume,vwap,transactions,timestamp] (epoch-only, NO datetime column, and 'transactions' instead of 'trade_count') - schema variant tracks naming convention 1:1 in every case checked.",53749,1304481890,"2806 ticker directories. In a full-corpus scan (not sampled) of all 2806 tickers, 87 ticker+date pairs exist under BOTH naming conventions simultaneously (e.g. ABVE 2025-10-23, AMTD 2025-10-31, BFLY 2025-10-31). Spot check (ABVE 2025-10-23): both files have identical row count (1631) and identical min/max timestamps - true duplicate content re-ingested under a second pipeline/schema, not merely a naming quirk. 2803 of 2806 tickers overlap with minute/.","2025-01-03 to 2025-11-22 only (226 distinct calendar dates, full corpus scan) - markedly narrower than minute/'s 2020-2025 span. second10/ appears to be a much newer/shorter-lived collection effort than minute/, or historical 10-second data for pre-2025 events was never collected/retained.","Two structural issues, both matching the prior audit's diagnostic pattern: (1) multiple naming conventions coexist for the same resolution within the same ticker folders (not just directory-wide, but interleaved file-by-file within individual ticker dirs); (2) confirmed exact duplicate ticker+date pairs (87 found, full scan) where the same trading session was collected twice under different pipelines/schemas with no evident deconfliction - both copies remain on disk. The schema variant (presence/absence of 'datetime' column, 'trade_count' vs 'transactions') is a perfect proxy for which naming convention produced the file, indicating two distinct, non-interoperable collector versions were both run and neither superseded the other's output. Unlike minute/, second10/ does NOT contain an embedded non-candle trades/ subfolder - this issue is specific to minute/."
data/illiquid_tests/,"1-minute bars (one-off illiquidity test artifact, not part of the main minute/ corpus)",parquet,"{TICKER}_{YYYY-MM-DD}_minute_data.parquet plus derived variants _minute_data_cleaned.parquet and _minute_data_with_flags.parquet, columns [t,o,h,l,c,v,vw,n,datetime_utc,datetime,hour,minute,session] (abbreviated OHLCV column names, distinct from both data/daily and data/minute conventions). Directory also holds a co-located OLMA_..._trades.parquet (tick data, out of scope) and 5 PNG chart exports (not data).",3,237720,"Single ticker: OLMA. No other tickers present.","Single session: 2025-11-18 (912 rows in the base file per file inspection).","Small, self-contained, single-symbol/single-day test directory, structurally isolated from the main daily/minute/second10 corpora - looks like a one-off illiquidity/anomaly-detection experiment (filenames and companion PNGs referencing 'anomaly_cleaning', 'anomaly_flagged', 'footprint_chart' support this). Uses yet a fourth distinct column-naming convention for OHLCV (o/h/l/c/v/vw/n abbreviated) not seen anywhere else in the candle corpus - worth noting as a fourth schema dialect if this directory is ever merged into the main minute/ corpus."
